#!/bin/sh
# Blocks a commit that contains anything credential-shaped.
# Enable once per clone:  git config core.hooksPath .githooks

python scripts/secret-scan.py --staged || {
  echo
  echo "Commit blocked by .githooks/pre-commit."
  echo "Override only if you are certain:  git commit --no-verify"
  exit 1
}
